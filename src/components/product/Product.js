import React from 'react'

export const Product = () => {
  return (
    /* {shoes.map((shoes, i) => (
        <div key={i}>
          <p>{shoes.nombre}</p>
          <img src={shoes.img} alt={shoes.name} />
        </div>
      ))} */
      <div>Hola uwu</div>
  )
}

export default Product;
